---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Thank You | Garden Edge" description="Thank you for your enquiry. We will be in touch shortly.">
    <!-- Background Texture -->
    <div class="fixed inset-0 pointer-events-none opacity-[0.4] z-[100] bg-[url('https://www.transparenttextures.com/patterns/concrete-wall.png')] mix-blend-multiply"></div>

    <main class="flex min-h-screen flex-col items-center justify-center font-sans text-slate-800 bg-slate-50 px-6">
        
        <div class="max-w-2xl w-full bg-white rounded-[2rem] shadow-2xl p-8 md:p-16 border border-emerald-100 text-center relative overflow-hidden">
            
            {/* Success Icon */}
            <div class="w-24 h-24 bg-amber-100 rounded-full flex items-center justify-center mx-auto mb-8 text-amber-600 animate-bounce">
                <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
            </div>

            <h1 class="text-4xl md:text-5xl font-bold text-slate-900 mb-6">Enquiry Received!</h1>
            
            <p class="text-xl text-slate-500 mb-10 leading-relaxed">
                Thanks for reaching out to <span class="text-amber-700 font-bold">Garden Edge</span>. 
                We have received your details and one of our team members will be in touch shortly to discuss your project.
            </p>

            <div class="space-y-4">
                <a href="/" class="inline-block px-8 py-4 bg-slate-900 text-white rounded-xl font-bold text-lg hover:bg-amber-700 transition-colors shadow-lg hover:shadow-orange-200 w-full sm:w-auto">
                    Return Home
                </a>
            </div>

            {/* Decorative Background Elements */}
            <div class="absolute -top-20 -left-20 w-64 h-64 bg-amber-50 rounded-full blur-3xl opacity-50 pointer-events-none"></div>
            <div class="absolute -bottom-20 -right-20 w-64 h-64 bg-amber-50 rounded-full blur-3xl opacity-50 pointer-events-none"></div>
        </div>

    </main>
</Layout>
